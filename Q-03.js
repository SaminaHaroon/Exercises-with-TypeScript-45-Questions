"use strict";
//Name Cases: Store a person’s name in a variable, and then print that
//person’s name in lowercase, uppercase, and titlecase.
let myName = "Samina haroon";
console.log(myName.toLowerCase()); //Small latters
console.log(myName.toUpperCase()); //Capital letters
console.log(myName.charAt(0).toUpperCase() + myName.slice(1).toLowerCase()); //First letter capital
